node_modules
.env
**/.DS_Store
/docs

# Playwright MCP test artifacts
.playwright-mcp/

# Screenshot artifacts from browser testing
*.png
*.jpg

# ...but the generated brand icons are real assets, not artifacts
!frontend/public/*.png

# Claude Code scratch. Worktrees are whole nested checkouts; settings.local is
# per-machine. A shared .claude/settings.json stays trackable.
.claude/worktrees/
.claude/settings.local.json
